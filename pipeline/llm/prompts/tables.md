Produza de 1 a 3 tabelas de referência que resumam os conceitos técnicos, notação formal ou variantes de comportamento discutidas no artigo abaixo — no estilo de uma tabela de "conceito × definição" ou "predicado × significado" (2-3 colunas, N linhas). Só gere uma tabela de notação formal se `formal_notation_present` for verdadeiro. Cada tabela tem uma legenda curta (`caption`), os nomes das colunas e as linhas como listas de strings (mesma quantidade de células que colunas).

Análise do artigo:
{paper_analysis_json}
